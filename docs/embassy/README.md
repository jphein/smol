# The Embassy campaign documents (#335 / #347)

The CANONICAL, tracked copies of the port-planning documents — the recon spec, the delta map,
the risk register, the radio-ownership decision (rev-2 + its adversarial review), the Phase-1
exec log, and PHASE3-PLAN. Working copies may exist under `scratch/embassy-port/` (untracked);
when they diverge, THIS directory is the record.

Deliberately NOT here: the ~30 original phase specs/exec logs from the July campaign — they
carry LAN topology and live only in the private tarball named on #335. Everything in this
directory is topology-clean by construction and re-scanned at commit time.

Line numbers in these documents rot (three different bases were found across them on
2026-08-25 — see PHASE3-PLAN's correction log). The binding anchors are fn names + declared
counts, per PHASE3-PLAN's anchoring policy.
